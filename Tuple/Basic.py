#tuple is like list only but the elements are
#ordered
#imutable that means we cannot change the value once it is created
#indexing is posssible
#syntac for tuple--> ()

a=(1,2,3,4,5)
print(a[1])#--> Indexing

# a.append(1) --> not possible

#we use tuple because it is safety than list
